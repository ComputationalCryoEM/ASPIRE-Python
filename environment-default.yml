name: 'aspire'

channels:
  - conda-forge
  - defaults

dependencies:
  - pip
  - python=3.10
  - numpy=1.25.0
  - scipy=1.13.1
  - scikit-learn
  - scikit-image
  - finufft=2.5.0  # must be installed via conda if using (conda) optimized backends
name: 'aspire'

channels:
  - conda-forge
  - defaults

dependencies:
  - pip
  - python=3.10
  - numpy=1.25.0
  - scipy=1.13.1
  - finufft=2.5.0  # must be installed via conda if using (conda) optimized backends
  - scikit-learn
  - scikit-image
  - libblas=*=*accelerate
